# TODO

## Remaining Migration Items

- Broaden Java parity tests against the upstream `.docx` fixture corpus.
- Add a Node-vs-Java comparison script under `scripts/` when the Java output is ready for routine upstream drift checks.
- Review whether Maven plugin support should remain out of scope or become a Java-side extension after runtime parity stabilizes.
- Add GitHub Release asset workflow after the jar/package contract is finalized.
